# Nightly browser repairs

Follow-up to scheduled run35554299044, related to `01-platform-infrastructure.md#T-05.05.01`. Branch `codex/nightly-browser-repairs` started from reviewed PR #329 HEAD `c85bb7d77f4db20af70b9d9db310c2fa88c37c8d`. That PR is now verified merged as `f1d74a3d8a4962f542c2c2d80f44a0ffd7512c21`; this batch is rebased onto it. This repairs browser validation, not every separate nightly-schedule requirement. No supervisor state is changed.

The reproduction found browser-specific fixture assumptions and unresolved CRM contrast checks. Preserve every browser project and accessibility requirement. Do not disable failing tests or accept unresolved contrast as a pass.

Implemented so far:

- Give the theme fixture the application's device-width viewport. The missing setting produced a 980px layout on a 393px mobile viewport and incorrect toast positioning assertions.
- Allow equivalent quoted CSS font-family serialization and subpixel separator measurements.
- Supply a portable synthetic paste payload. A direct runtime probe showed Firefox discards the DataTransfer passed to ClipboardEvent; Chromium and WebKit retain it. Existing OTP request, normalization, error, reset and focus assertions remain.
- Assert registration benefits are hidden below the existing 768px responsive breakpoint and visible above it, while retaining the full registration interaction.
- Open the mobile navigation to check its visible labels, then close it before checking form interactions. Content and navigation each retain violation and incomplete-contrast checks.

Validation checkpoint: the first focused run passed38 of39 cases across Firefox, WebKit and mobile Chrome. The only failure was WebKit's equivalent unquoted Inter Variable font serialization, now corrected. A second focused run is checking fonts, navigation contrast and native CSP transport. Full validation, CRM contrast repairs, independent review and CI remain.

Follow-up validation: all15 font/navigation cases pass across Firefox, WebKit and mobile Chrome. Native CSP tests now verify the document's session cookie independently, then assert observed engine transport: Firefox omits report cookies, WebKit uses fetch destination empty, and Chromium uses report with the same-origin cookie. All10 signed-in/signed-out CSP cases pass across all five browser projects. Server session-CSRF protection is unchanged.

CRM geometry confirms vertical clipping by the scrollable main content: wrapped mobile tabs extend below its bottom edge, and the session table can be below the desktop viewport. The shared contrast helper now proves clipping against viewport and scroll ancestors before revealing each affected node and requiring a clean contrast rescan. It retains all measured violations and rejects incomplete results that cannot be explained by clipping.

The Persian Firefox session reference also touched the table's right edge with fractional text geometry. Scrolling alone did not resolve it. Horizontal cell padding does, and the Persian table was visually inspected. The five-project CRM run passed39/40. Mobile Safari's English dark case timed out during initial navigation and context shutdown, before any network record or CRM assertion. Repeating the four mobile Safari variants reproduced the fourth-case startup timeout. Investigate it separately from contrast; do not call the whole suite clean. Final batch coverage, independent review and CI remain.

The local Safari timeout reproduced in both four-case repetitions, while the same English dark case passed twice in fresh workers. A minimal six-context Safari reuse probe passed. No speculative browser fixture workaround is introduced. Validate the complete browser workflow on Linux and keep the local limitation explicit. Types, targeted lint and production build pass.

PR #330's first exact-head review found that the mobile navigation was hidden when its second scroll position was assigned. The repair now opens the menu, scrolls it, verifies the final link is included in the visible-label scan, and checks positive scrolling when overflow exists. All20 navigation contrast cases pass across the five browser projects. Review artifact: https://github.com/barghsadev/barghsa-core/pull/330#issuecomment-5755304903. Re-review and checks on the fix commit remain required.


Safari follow-up checkpoint: Linux run 35560053066 passed all 796 cases in Firefox and mobile Chrome. Mobile Safari completed with failures; desktop WebKit hit the 60-minute job limit. Do not merge against the prior approval or claim the browser workflow passed.

The upload tests now receive actual HTTP upload bytes instead of relying on WebKit's unavailable intercepted File body. All 20 upload cases passed across five projects. A repeated cookie probe found stale immediate document cookies after 48/50 mocked WebKit responses and 0/50 native same-origin responses. The CSRF fixture now synchronizes its readable cookie before completing the mocked response; it retains request-token assertions. A cross-port native-response experiment was discarded because it changed default credentials behavior. The real HTTP session-cookie test still verifies production cookie helpers and now polls the browser-context cookie snapshot.

The remaining Linux mobile failures involved rich-text selection and notices intercepting repeated form clicks. Tests now select editor text with keyboard selection events and dismiss notices through their accessible close controls. The shared-control test separates each branding color into its own case, preserving all assertions within the per-test timeout. The nightly workflow runs two complementary shards for each Safari project, preserving all tests and retaining Firefox/mobile Chrome as single jobs. These follow-ups require a fresh exact-head review and a successful new Linux run.

The combined follow-up run passed 134/135 cases across all five browsers, including all CSRF, session-cookie and notice-dismissal flows. The remaining Firefox Persian rich-text case showed direct DOM selection racing the editor's selection state; keyboard selection replaced it. The subsequent focused run covers both rich-text flows, all newly separated theme-color cases, and the catalogue lifecycle across Firefox and both Safari projects.

Downloaded Linux WebKit traces show catalogue progress through many real password confirmations until the default 30-second limit, with each confirmation taking roughly 0.8 seconds on that runner. That single full-lifecycle scenario now has a 60-second limit. Shared-control traces likewise exhausted the total case duration; individual color cases retain all hover, focus, contrast, and reduced-motion assertions. Type checking, scoped lint, browser-selection/result-checker tests, backlog validation, and workflow YAML/shard completeness checks pass.

Final focused follow-up: all54 catalogue, rich-text and separately parameterized theme cases pass across Firefox, desktop WebKit and mobile Safari. Upload verification remains20/20 across all five projects. The earlier134/135 run and the successful focused repair are reported separately; no failed run is reclassified as passing.


Second Linux follow-up at reviewed head39472476: both Safari shard2 jobs finished with flaky outcomes and correctly failed the workflow. The native-cookie trace proves production helpers emitted Secure cookies while the fixture used plain HTTP. The fixture now uses a temporary local TLS certificate, explicitly exercises production cookie flags, asserts Secure, and restores its environment afterward. All15 runs across five browser projects pass. Verification-mode and invitation tests were the two remaining direct CSRF-cookie mocks; both now use the established synchronized-cookie fixture. All30 focused login/invitation/verification cases pass across five projects.

The mobile Safari login first-attempt trace separately reports `WebKit encountered an internal error` on the app document navigation; the retry and local focused case pass. No retry wrapper, skip, quarantine, or weakened assertion was added. A new Linux run must verify it remains clean. Run35564493501 finished: Firefox, mobile Chrome and mobile Safari shard1 passed; all other Safari shards failed on flaky outcomes. Earlier approval and coverage apply to39472476, not this follow-up.

The final desktop Safari shard exposed transient contrast measurements after reopening the archive dialog. Waiting only for the enabled button's opacity did not cover the popup's fade animation. The test now waits for the popup/backdrop opacity and animations to settle before the unchanged strict accessibility scan. All20 locale/theme cases pass across five browsers, and the previously flaky Persian dark case passes five repetitions in each Safari project. Scoped lint, web type checking and backlog validation pass. The initial development-server attempt timed out before tests began; the successful runs use the production server. Fresh exact-head review and Linux validation remain required before merge.

Run35566960951 at a9cb8548 passed five of six jobs, including both mobile Safari shards, Firefox and mobile Chrome. Desktop Safari shard1 reported407 expected cases and one flaky chargeback-dashboard case. Its trace contains two successful warning responses and no503: advancing the fake clock started a successful poll before the fixture changed its response, then the next tick was skipped while that request remained pending. This is fixture ordering, not evidence of a production polling failure. The fixture now sets the error response before advancing time and verifies recovery before each invalid-payload scenario, so stale error UI cannot satisfy later assertions. All20 cases pass across five projects, plus five repetitions of the previously flaky desktop Safari case. Lint, production build and backlog validation pass. The failed full run is retained as diagnostic evidence; the new commit still needs exact-head review and CI.
