# Continue here

Read [fix-plan.md](fix-plan.md) and [progress.json](progress.json), then only the selected requirements/evidence. Directory reconciliation is complete; do not routinely reread the archive.

Workspace `/Users/majid/www/barghsa/barghsa-core`, branch `codex/audit-fixes`. Check actual HEAD/worktree. Latest product `ab10771` closes local staff creation, role assignment and staff-list acceptance. It fixes creator-only initial roles, temporary-password guarantees and dark-theme staff content. [Step review](evidence/step-reviews.json#R01-staff-task-acceptance) records220 API/unit cases,12 distinct browser cases, scoped light/dark scans, quality checks and41 route budgets. Failed runs and exact reuse are retained. No broad checkpoint renewal.

Next: `02-auth-users-admin.md#T-05.04.04` agent-role permissions, current step-up audit/deadlines and UI recovery. Reuse8904b03 credential checks. Then remaining agent/invitation/ownership and domain/CSRF dispositions. Lost-contact policy remains pending; do not ask again.

Counts:53 recorded verified /18 partial /251 pending claims;12 older verified-record source drifts remain. These are review counts, not coding effort. Saved inventory:301 PRs,58 skips. PR129/130/218 have current task dispositions; four deferral statements are reconciled. GitHub refresh awaits access.

Use `rtk` and prefer codebase-memory. Freeze scope and reuse valid evidence. Build the frontend before production-browser checks: the runner serves existing dist and does not build it. Verify asset hashes. Do not overlap API/shared builds with consumer typechecks or browser fixtures. Local edits and explicit commits only; no push, PR publication/merge, scheduler or deployment action.
