# Continue here

Read [fix-plan.md](fix-plan.md), then [progress.json](progress.json). Follow `next_action`; read selected requirements/evidence. Directory-wide reconciliation is complete.

Workspace `/Users/majid/www/barghsa/barghsa-core`, branch `codex/audit-fixes`. Check actual HEAD/worktree. Latest product `455ed61` rejects invalid/future step-up times, keeps credentials out of guard logs, logs all CSRF rejection paths and returns the required step-up flag. 83 distinct API cases in 7 files pass in the final run, plus final types/lint/format. Initial failing tests and the corrected fixture typecheck are retained in `evidence/step-reviews.json#R01-guard-boundaries`.

Next: [security-route-review.md](security-route-review.md). Registered route matrix complete: 342 routes, 203 unsafe methods, 120 guarded step-up routes. Verify staff creation with role grants and verification-provider configuration, then domain audit/UI recovery and public-auth/callback/telemetry dispositions. Registration is not handler acceptance. [Session caller matrix](session-caller-review.md) retains credential effects and ten exact method comparisons. F07 retains whole invitation/ownership acceptance. Lost-contact policy question remains pending; do not ask again.

Counts remain 50 verified /18 partial /254 pending records, not coding progress. Twelve verified records have evidence drift to reconcile; drift alone is not a defect. GitHub refresh awaits access. No broad checkpoint renewal.

Use `rtk` and prefer codebase-memory. Keep scope fixed and checks focused. Reuse valid evidence. Do not overlap consumer typechecks with shared/API rebuilds, or builds with browser fixtures. Explicit staging/local commits only; no push, PR publication/merge, scheduler or deployment action.
