# Continue here

Read [fix-plan.md](fix-plan.md), then [progress.json](progress.json). Follow `next_action`; read only the selected requirements and relevant evidence. The requested directory-wide reconciliation is complete. Do not routinely reload the archive.

Workspace `/Users/majid/www/barghsa/barghsa-core`, branch `codex/audit-fixes`. Check actual HEAD and worktree. Latest product is `c26aad7`: approximate session countries, active-list filtering and truthful read failures are locally verified. Continue R01 caller review with revoke-all password confirmation and mutation atomicity, then the remaining rotation/revocation/CSRF/sensitive-action matrices.

Recent verified steps: OTP-first reset `9827e96`, current-session refresh `218a3a3`, cookie scope/policy `3ff2920`, country location `c26aad7`. Exact scopes, source hashes and logs live in `evidence/step-reviews.json`. The latest step passed 68 API cases, 16 distinct browser cases across focused runs, types/lint, all 41 route budgets, contract, frozen install and an isolated API deployment lookup. This does not renew the broad checkpoint, coverage or production images.

Counts remain 50 verified / 18 partial / 254 pending acceptance records, not coding progress. Twelve verified records have source/dependency changes to reconcile in `progress.json.evidence_refresh_queue`; changed files alone do not prove defects. New IP database/parser maintenance and updates belong to R05. The lost-contact policy question remains pending; do not ask again. GitHub inventory refresh awaits available access.

Use `rtk` and prefer codebase-memory. Keep scope fixed and save compact evidence after each bounded step. Reuse matching evidence. Do not overlap any consumer typecheck with test-driven shared/API rebuilds, or builds with browser fixtures. Reserve the broad regression checkpoint for V02 unless changes justify it earlier. Stage explicit paths. Local implementation/commits only; no remote operation or feature-loop dispatch.
