# Continue here

Read [fix-plan.md](fix-plan.md), then [progress.json](progress.json). Follow `next_action`; read only selected requirements and evidence. The requested directory-wide reconciliation is complete.

Workspace `/Users/majid/www/barghsa/barghsa-core`, branch `codex/audit-fixes`. Check actual HEAD/worktree. Latest product `5543cc2` makes invitation acceptance atomic with current-session rotation, other-session invalidation and audit. Original deadlines are checked after lock/write waits.64 distinct API cases and4 desktop/mobile browser cases pass across focused runs, plus types/lint/format and unchanged OpenAPI. Exact scopes, source hashes and failures are saved in `evidence/step-reviews.json#R01-invitation-credentials`.

Next: CSRF route/alternative and sensitive-action matrices for T-02.02.03/T-02.02.04, then task-wide session acceptance. [Caller matrix](session-caller-review.md) records covered triggers and ten reusable method comparisons. F07 retains full invitation/decline/details, agent/ownership and profile-context acceptance. The lost-contact policy question remains pending; do not ask again.

Counts remain50 verified /18 partial /254 pending records, not coding progress. Twelve verified records have source/dependency drift to reconcile in progress.json; drift alone does not prove defects. GitHub inventory refresh awaits access. No broad checkpoint, coverage, route-budget or production-image renewal is claimed.

Use `rtk` and prefer codebase-memory. Keep scope fixed, checks focused and output small. Do not overlap consumer typechecks with shared/API rebuilds, or builds with browser fixtures. Reserve broad regression for V02 unless changed behavior warrants it sooner. Explicit staging and local commits only; no push, PR publication/merge, scheduler or deployment action.
