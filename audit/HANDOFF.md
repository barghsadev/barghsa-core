# Continue here

Read [fix-plan.md](fix-plan.md) and [progress.json](progress.json), then only selected requirements/evidence. Directory reconciliation is complete; do not routinely reread the archive.

Workspace `/Users/majid/www/barghsa/barghsa-core`, branch `codex/audit-fixes`. Check actual HEAD/worktree. Latest product `417ab33` repairs agent-role changes/removal after request-time authorization: current session/CSRF/deadlines and grants hold through commit; audit records verified time/correlation; self-mutations return to login. [Step review](evidence/step-reviews.json#R01-agent-sensitive-actions) records51 distinct API and32 production-browser cases, quality checks and41 unchanged budgets. Failed fixtures and exact reuse are retained. No broad checkpoint renewal.

R01: repair confirmed invitation authority gaps in AgentsService.createInvitation/withdrawInvitation. Creation checks owner/Manager before its transaction; withdrawal also accepts the original inviter after their role is removed. Bind both writes to current profile/account/session/grants and audit correlation, preserving non-step-up invitation policy. Then finish current domain-role/conditional-UI and invitation/ownership acceptance. Lost-contact policy remains pending; do not ask again.

Discovery already done: shared role matrix is consumed through activeProfileSql and explicit service authorization; no actual RequireAgentPermission route decorators were found. PR134 was a first framework slice. Existing context/order/address/profile-write HTTP suites contain permission cases; do not duplicate them. Creation/withdrawal and their unit tests were read; the existing original-inviter success test encodes the withdrawal defect.

Counts remain53 recorded verified /18 partial /251 pending claims;12 older verified-record source drifts remain. Review counts are not coding effort. Saved inventory:301 PRs,58 skips. Staff creation/role assignment/list acceptance retainsab10771 evidence. GitHub refresh awaits access.

Use `rtk` and prefer codebase-memory. Freeze scope and reuse valid evidence. Build the frontend before production-browser checks: the runner serves existing dist. Verify asset hashes. Playwright global setup rebuilds API/shared, so finish browser runs before consumer typechecks. Do not overlap shared/API builds with browser fixtures. Local edits and explicit commits only; no push, PR publication/merge, scheduler or deployment action.
